---
phase: 08-tag-tree-graph-enhancement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend-go/internal/domain/models/topic_graph.go
  - backend-go/internal/platform/database/postgres_migrations.go
  - backend-go/internal/domain/topicextraction/tagger.go
  - backend-go/internal/domain/topicanalysis/abstract_tag_service.go
autonomous: true
requirements: []

must_haves:
  truths:
    - "TopicTag 模型包含 description 字段"
    - "新文章标签创建时通过 LLM 生成 description"
    - "抽象标签提取时同时生成 description"
    - "description 字段在 API 响应中可见"
  artifacts:
    - path: "backend-go/internal/domain/models/topic_graph.go"
      provides: "TopicTag.Description 字段"
      min_lines: 1
    - path: "backend-go/internal/platform/database/postgres_migrations.go"
      provides: "description 列 migration"
      min_lines: 10
    - path: "backend-go/internal/domain/topicanalysis/abstract_tag_service.go"
      provides: "callLLMForAbstractName 返回 name+description, ExtractAbstractTag 设置 description"
      exports: ["ExtractAbstractTag"]
  key_links:
    - from: "tagger.go:findOrCreateTag()"
      to: "LLM description 生成"
      via: "async goroutine after tag creation"
      pattern: "go generateTagDescription"
    - from: "abstract_tag_service.go:ExtractAbstractTag()"
      to: "TopicTag.Description"
      via: "扩展 callLLMForAbstractName 返回 description"
      pattern: "abstractTag.Description = result.Description"
---

<objective>
**Plan 01: 后端 Description 字段 + 文章标签 Description 生成**

Purpose: 为 TopicTag 模型添加 description 字段，并在新文章标签创建时通过 LLM 自动生成 description（D-01）。
Output: TopicTag 模型更新 + migration + findOrCreateTag 集成 description 生成。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-tag-tree-graph-enhancement/08-CONTEXT.md
@.planning/phases/08-tag-tree-graph-enhancement/08-RESEARCH.md
@backend-go/internal/domain/models/topic_graph.go
@backend-go/internal/domain/topicextraction/tagger.go
@backend-go/internal/domain/topicanalysis/abstract_tag_service.go
@backend-go/internal/platform/database/postgres_migrations.go
</context>

<interfaces>
<!-- Key types and contracts from codebase -->

From backend-go/internal/domain/models/topic_graph.go:
```go
type TopicTag struct {
    ID           uint      `gorm:"primaryKey" json:"id"`
    Slug         string    `gorm:"size:120;not null;index:idx_topic_tags_category_slug" json:"slug"`
    Label        string    `gorm:"size:160;not null" json:"label"`
    Category     string    `gorm:"size:20;not null;default:keyword;index:idx_topic_tags_category_slug" json:"category"`
    // ... other fields
    Status       string    `gorm:"size:20;not null;default:active;index" json:"status"`
    CreatedAt    time.Time `json:"created_at"`
    UpdatedAt    time.Time `json:"updated_at"`
}
```

From backend-go/internal/domain/topicanalysis/abstract_tag_service.go:
```go
// callLLMForAbstractName — LLM JSON mode call pattern
router := airouter.NewRouter()
req := airouter.ChatRequest{
    Capability:  airouter.CapabilityTopicTagging,
    Messages:    []airouter.Message{{Role: "system", Content: "..."}, {Role: "user", Content: prompt}},
    JSONMode:    true,
    Temperature: func() *float64 { f := 0.3; return &f }(),
}
result, _ := router.Chat(ctx, req)
// Parse JSON response

// ExtractAbstractTag — creates abstract tag with embedding
abstractTag = &models.TopicTag{Slug: slug, Label: abstractName, Category: category, Source: "abstract", Status: "active"}
tx.Create(abstractTag)
```

From backend-go/internal/domain/topicextraction/tagger.go:
```go
// findOrCreateTag — creates new tag at line 242-262
newTag := models.TopicTag{
    Slug: slug, Label: tag.Label, Category: category,
    Kind: kind, Icon: tag.Icon, Aliases: string(aliasesJSON),
    IsCanonical: true, Source: source,
}
database.DB.Create(&newTag)
// Embedding generated async via go generateAndSaveEmbedding(es, &newTag)
```

From backend-go/internal/platform/database/postgres_migrations.go:
```go
// Migration pattern — append to migrations slice
type Migration struct {
    Name string
    Fn   func(db *gorm.DB) error
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: TopicTag 模型添加 description 字段 + migration</name>
  <read_first>
    backend-go/internal/domain/models/topic_graph.go
    backend-go/internal/platform/database/postgres_migrations.go
  </read_first>
  <files>
    backend-go/internal/domain/models/topic_graph.go
    backend-go/internal/platform/database/postgres_migrations.go
  </files>
  <action>
    1. 在 TopicTag struct 中添加 `Description` 字段：
       ```go
       Description string `gorm:"type:text" json:"description"`
       ```
       放在 `Aliases` 字段之后（line 48 之后）。

    2. 在 postgres_migrations.go 中添加 migration：
       ```go
       {Name: "add_topic_tag_description", Fn: func(db *gorm.DB) error {
           return db.Exec("ALTER TABLE topic_tags ADD COLUMN IF NOT EXISTS description TEXT").Error
       }},
       ```
       追加到现有 migrations 列表末尾。
  </action>
  <acceptance_criteria>
    - TopicTag struct 包含 `Description string` 字段，json tag 为 `description`
    - postgres_migrations.go 包含 `add_topic_tag_description` migration
    - `go build ./...` 通过
  </acceptance_criteria>
  <verify>
    <automated>cd backend-go && go build ./...</automated>
  </verify>
  <done>TopicTag 模型新增 description 字段，migration 已添加</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: findOrCreateTag 创建新标签时生成 description</name>
  <read_first>
    backend-go/internal/domain/topicextraction/tagger.go
    backend-go/internal/domain/topicanalysis/abstract_tag_service.go
  </read_first>
  <behavior>
    - 新标签创建后，异步调用 LLM 生成 description
    - LLM 输入：标签 label + slug + category + 关联摘要的 title/summary
    - LLM 输出：JSON {"description": "一段简介"}
    - description 存入 TopicTag.Description
    - 失败时不阻塞标签创建，仅 log warning
  </behavior>
  <files>
    backend-go/internal/domain/topicextraction/tagger.go
  </files>
  <action>
    1. 在 tagger.go 中新增 `generateTagDescription` 函数：
       ```go
       func generateTagDescription(tagID uint, label, category, articleContext string) {
           defer func() { if r := recover(); r != nil { log.Printf("[WARN] generateTagDescription panic: %v", r) } }()
           router := airouter.NewRouter()
           prompt := fmt.Sprintf(`Generate a concise description (1-2 sentences) for this tag.
Tag: %q
Category: %s
Context from article: %s

Respond with JSON: {"description": "your answer"}`, label, category, articleContext)
           req := airouter.ChatRequest{
               Capability: airouter.CapabilityTopicTagging,
               Messages: []airouter.Message{
                   {Role: "system", Content: "You are a tag taxonomy assistant. Respond only with valid JSON."},
                   {Role: "user", Content: prompt},
               },
               JSONMode:    true,
               Temperature: func() *float64 { f := 0.3; return &f }(),
           }
           result, err := router.Chat(context.Background(), req)
           if err != nil {
               log.Printf("[WARN] Description LLM call failed for tag %d: %v", tagID, err)
               return
           }
           var parsed struct { Description string `json:"description"` }
           if err := json.Unmarshal([]byte(result.Content), &parsed); err != nil || parsed.Description == "" {
               log.Printf("[WARN] Failed to parse description for tag %d", tagID)
               return
           }
           database.DB.Model(&models.TopicTag{}).Where("id = ?", tagID).Update("description", parsed.Description)
       }
       ```

    2. 修改 `findOrCreateTag` 签名，接受 `articleContext string` 参数（从调用方传入摘要的 title + summary 片段）。

    3. 在新标签创建成功后（line 253 `database.DB.Create(&newTag)` 之后），添加异步 description 生成：
       ```go
       if articleContext != "" {
           go generateTagDescription(newTag.ID, tag.Label, category, articleContext)
       }
       ```

    4. 更新 `findOrCreateTag` 的所有调用方（TagSummary 等），传入 `articleContext`。在 TagSummary 中，从 summary 提取 title + summary 前 200 字符作为 context。

    5. 添加 import: `my-robot-backend/internal/platform/airouter`（如果不存在）。
  </action>
  <acceptance_criteria>
    - tagger.go 包含 `generateTagDescription` 函数
    - findOrCreateTag 接受 articleContext 参数
    - 新标签创建后 go generateTagDescription 被调用
    - `go test ./internal/domain/topicextraction/... -v` 通过
    - `go build ./...` 通过
  </acceptance_criteria>
  <verify>
    <automated>cd backend-go && go test ./internal/domain/topicextraction/... -v</automated>
  </verify>
  <done>新文章标签创建时异步生成 description，失败不阻塞</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API→LLM | LLM response 被解析为 description 存入数据库 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-08-01 | Tampering | LLM description injection | mitigate | description 存入前做长度限制（max 500 字符），不作为可执行内容渲染 |
| T-08-02 | Information Disclosure | LLM prompt 泄露文章内容 | accept | 本地部署，单用户，LLM 是可信 provider |
</threat_model>

<verification>
1. go build 通过
2. go test 通过
3. TopicTag.Description 字段存在
4. migration 文件包含 add_topic_tag_description
</verification>

<success_criteria>
- TopicTag.Description 字段已添加到模型
- migration 已添加到 postgres_migrations.go
- generateTagDescription 函数实现
- findOrCreateTag 集成 description 生成
- 所有调用方更新完毕
</success_criteria>

<output>
After completion, create `.planning/phases/08-tag-tree-graph-enhancement/08-01-SUMMARY.md`
</output>
