---
phase: 08-tag-tree-graph-enhancement
plan: 04
type: execute
wave: 3
depends_on:
  - 08-02
files_modified:
  - front/app/features/topic-graph/utils/buildTopicGraphViewModel.ts
  - front/app/features/topic-graph/components/TopicGraphCanvas.client.vue
  - front/app/api/topicGraph.ts
  - front/app/features/topic-graph/components/TopicGraphSidebar.vue
autonomous: true
requirements: []

must_haves:
  truths:
    - "抽象标签节点在图谱中用外发光效果区分（保持类别颜色）"
    - "点击抽象标签节点显示详情面板（子标签列表 + 文章时间线）"
    - "详情面板支持按子标签筛选时间线"
  artifacts:
    - path: "front/app/features/topic-graph/utils/buildTopicGraphViewModel.ts"
      provides: "TopicGraphSceneNode 增加 isAbstract 字段"
    - path: "front/app/features/topic-graph/components/TopicGraphCanvas.client.vue"
      provides: "抽象标签节点 emissive 材质效果"
    - path: "front/app/features/topic-graph/components/TopicGraphSidebar.vue"
      provides: "抽象标签详情面板（子标签 + 时间线筛选）"
  key_links:
    - from: "TopicGraphCanvas.client.vue"
      to: "buildTopicGraphViewModel.ts"
      via: "isAbstract flag on node"
      pattern: "isAbstract.*emissive"
    - from: "nodeClick event"
      to: "TopicGraphSidebar.vue"
      via: "abstract tag detail panel"
      pattern: "nodeClick.*abstract"
---

<objective>
**Plan 04: 图谱抽象标签可视化 + 点击详情面板**

Purpose: 在 3D 图谱中用外发光效果区分抽象标签节点（D-05），点击抽象标签显示子标签列表和文章时间线（D-06）。
Output: buildTopicGraphViewModel 扩展 isAbstract + TopicGraphCanvas emissive 材质 + 详情面板。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-tag-tree-graph-enhancement/08-CONTEXT.md
@.planning/phases/08-tag-tree-graph-enhancement/08-RESEARCH.md
@front/app/features/topic-graph/utils/buildTopicGraphViewModel.ts
@front/app/features/topic-graph/components/TopicGraphCanvas.client.vue
@front/app/api/topicGraph.ts
</context>

<interfaces>
<!-- Key contracts from codebase -->

From front/app/features/topic-graph/utils/buildTopicGraphViewModel.ts:
```typescript
export interface TopicGraphSceneNode extends GraphNode {
  size: number
  accent: string
  x?: number; y?: number; z?: number
}

// Category colors:
const TOPIC_CATEGORY_ACCENTS: Record<TopicCategory, string> = {
  event: '#f59e0b',
  person: '#10b981',
  keyword: '#6366f1',
}
```

From front/app/features/topic-graph/components/TopicGraphCanvas.client.vue:
```typescript
// Props
interface Props {
  nodes: TopicGraphSceneNode[]
  edges: TopicGraphSceneEdge[]
  activeNodeId?: string | null
  // ...
}

// Emits
emit('nodeClick', node: TopicGraphSceneNode)
```

From front/app/api/topicGraph.ts:
```typescript
// GraphNode — API response node shape
export interface GraphNode {
  id: string
  label: string
  slug: string
  category: TopicCategory
  kind?: string
  score: number
  // is_abstract?: boolean — may exist from Phase 7
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 图谱节点抽象标签外发光效果</name>
  <read_first>
    front/app/features/topic-graph/utils/buildTopicGraphViewModel.ts
    front/app/features/topic-graph/components/TopicGraphCanvas.client.vue
    front/app/api/topicGraph.ts
  </read_first>
  <behavior>
    - GraphNode API 响应包含 is_abstract 字段
    - TopicGraphSceneNode 增加 isAbstract: boolean
    - buildTopicGraphViewModel 传递 isAbstract 到 scene node
    - TopicGraphCanvas 中抽象标签节点使用 emissive 材质（发光效果）
    - 颜色保持类别颜色（event=#f59e0b 等），发光用同色更亮版本
  </behavior>
  <files>
    front/app/features/topic-graph/utils/buildTopicGraphViewModel.ts
    front/app/features/topic-graph/components/TopicGraphCanvas.client.vue
    front/app/api/topicGraph.ts
  </files>
  <action>
    1. 在 `front/app/api/topicGraph.ts` 的 GraphNode 接口中确认或添加 `is_abstract` 字段：
       ```typescript
       export interface GraphNode {
         // ... existing fields
         is_abstract?: boolean
       }
       ```

    2. 在 `buildTopicGraphViewModel.ts` 中：
       - 扩展 TopicGraphSceneNode：
         ```typescript
         export interface TopicGraphSceneNode extends GraphNode {
           // ... existing fields
           isAbstract: boolean
         }
         ```
       - 在构建节点时传递：
         ```typescript
         return {
           ...normalizedNode,
           size: buildNodeSize(normalizedNode),
           accent: resolveNodeAccent(normalizedNode),
           isAbstract: normalizedNode.is_abstract ?? false,
         }
         ```

    3. 在 `TopicGraphCanvas.client.vue` 中：
       - 找到 `buildNodeObject()` 或创建节点材质的函数。
       - 对于 `isAbstract === true` 的节点，使用 emissive 材质：
         ```javascript
         if (node.isAbstract) {
           // Add glow effect: emissive color = lighter version of accent
           const glowColor = lightenColor(node.accent, 0.3)
           nodeMaterial.emissive = new THREE.Color(glowColor)
           nodeMaterial.emissiveIntensity = 0.4
         }
         ```
       - 如果使用 force-graph 库，可能需要通过 `nodeThreeObject` 自定义节点渲染。
       - 添加 `lightenColor` 工具函数（将 hex 颜色提亮）。
  </action>
  <acceptance_criteria>
    - TopicGraphSceneNode 包含 isAbstract: boolean
    - buildTopicGraphViewModel 传递 isAbstract
    - 抽象标签节点有 emissive 发光效果
    - 非抽象标签节点样式不变
    - `pnpm exec nuxi typecheck` 通过
    - `pnpm build` 通过
  </acceptance_criteria>
  <verify>
    <automated>cd front && pnpm exec nuxi typecheck && pnpm build</automated>
  </verify>
  <done>抽象标签在图谱中有发光效果，保持类别颜色</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 点击抽象标签节点显示详情面板</name>
  <read_first>
    front/app/features/topic-graph/components/TopicGraphCanvas.client.vue
    front/app/api/abstractTags.ts
    front/app/api/topicGraph.ts
  </read_first>
  <behavior>
    - 点击抽象标签节点显示侧边/底部详情面板
    - 面板显示：子标签列表、文章时间线
    - 面板中可按子标签筛选时间线
    - 复用现有 TopicGraphSidebar 行为模式（或扩展它）
  </behavior>
  <files>
    front/app/features/topic-graph/components/TopicGraphSidebar.vue
    front/app/features/topic-graph/pages/TopicGraphPage.vue
  </files>
  <action>
    1. 检查现有 TopicGraphSidebar 或 TopicGraphPage 中节点点击后的面板逻辑。

    2. 在 `TopicGraphSidebar.vue`（或创建新的 `AbstractTagDetailPanel.vue`）中添加抽象标签详情：
       - 如果 TopicGraphSidebar 已存在，在其中添加条件分支：当点击的节点 `isAbstract === true` 时，渲染子标签列表 + 文章时间线。
       - 面板内容：
         ```html
         <div v-if="node.isAbstract">
           <h3>{{ node.label }}</h3>
           <p class="text-sm text-neutral-500">抽象标签</p>
           <!-- 子标签列表 -->
           <div class="mt-3">
             <h4 class="text-xs font-medium">子标签</h4>
             <div v-for="child in childTags" :key="child.id"
                  class="flex items-center gap-2 py-1 cursor-pointer"
                  @click="filterTimeline(child.id)">
               <span class="w-2 h-2 rounded-full" :style="{ background: child.accent }"></span>
               {{ child.label }}
             </div>
           </div>
           <!-- 文章时间线 -->
           <div class="mt-3">
             <h4 class="text-xs font-medium">文章时间线</h4>
             <div v-for="article in filteredArticles" :key="article.id" class="py-1 text-sm">
               {{ article.title }}
               <span class="text-xs text-neutral-400">{{ article.publishedAt }}</span>
             </div>
           </div>
         </div>
         ```

    3. 通过 API 获取子标签列表（复用 `abstractTags.ts:fetchHierarchy` 或新增专用 API）。

    4. 通过 API 获取该抽象标签关联的文章时间线（可能需要新增后端 endpoint 或复用现有文章查询 API）。

    5. 子标签点击筛选时间线：本地过滤已加载的文章数据。
  </action>
  <acceptance_criteria>
    - 点击抽象标签节点显示详情面板
    - 面板包含子标签列表
    - 面板包含文章时间线
    - 子标签点击可筛选时间线
    - `pnpm exec nuxi typecheck` 通过
    - `pnpm build` 通过
  </acceptance_criteria>
  <verify>
    <automated>cd front && pnpm exec nuxi typecheck && pnpm build</automated>
  </verify>
  <done>点击抽象标签节点弹出详情面板，可查看子标签和文章时间线</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API→UI | 节点数据来自后端，isAbstract 标记需验证 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-08-05 | Spoofing | 伪造 isAbstract 标记 | accept | 单用户部署，数据来源可信 |
</threat_model>

<verification>
1. pnpm exec nuxi typecheck 通过
2. pnpm build 通过
3. TopicGraphSceneNode 包含 isAbstract
4. 抽象标签节点有发光效果
5. 点击抽象标签显示详情面板
</verification>

<success_criteria>
- 抽象标签在图谱中用发光效果区分
- 点击抽象标签显示详情面板
- 详情面板显示子标签列表
- 详情面板显示文章时间线
- 子标签可筛选时间线
</success_criteria>

<output>
After completion, create `.planning/phases/08-tag-tree-graph-enhancement/08-04-SUMMARY.md`
</output>
