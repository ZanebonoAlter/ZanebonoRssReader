---
phase: 08-tag-tree-graph-enhancement
plan: 06
type: execute
wave: 4
depends_on:
  - 08-02
files_modified:
  - backend-go/internal/domain/topicanalysis/abstract_tag_service.go
  - backend-go/internal/domain/topicanalysis/handler.go
  - backend-go/internal/app/router.go
  - front/app/api/abstractTags.ts
  - front/app/features/topic-graph/components/TagHierarchyRow.vue
  - front/app/features/topic-graph/components/TagHierarchy.vue
autonomous: true
requirements: []

must_haves:
  truths:
    - "标签树节点支持手动调整到其他父节点"
    - "弹窗显示 embedding 相近的抽象层供选择归类"
    - "后端提供节点归类 API"
  artifacts:
    - path: "backend-go/internal/domain/topicanalysis/abstract_tag_service.go"
      provides: "ReassignTagParent 函数（移动标签到新父节点）"
    - path: "backend-go/internal/domain/topicanalysis/handler.go"
      provides: "POST /topic-tags/:id/reassign endpoint"
    - path: "front/app/features/topic-graph/components/TagHierarchyRow.vue"
      provides: "右键/操作菜单触发归类弹窗"
    - path: "front/app/features/topic-graph/components/TagHierarchy.vue"
      provides: "ReassignTagModal 组件（显示相近抽象层选择）"
  key_links:
    - from: "TagHierarchyRow.vue"
      to: "ReassignTagModal"
      via: "操作菜单触发"
      pattern: "reassign.*click"
    - from: "ReassignTagModal"
      to: "/topic-tags/:id/reassign"
      via: "POST API 调用"
      pattern: "reassign.*apiClient"
---

<objective>
**Plan 06: 标签树节点手动归类（后端 API + 前端弹窗）**

Purpose: 实现标签树节点手动调整到其他父节点（success criteria item 5）。后端提供归类 API，前端提供弹窗显示 embedding 相近的抽象层供选择。
Output: ReassignTagParent 后端函数 + POST endpoint + 前端归类弹窗 UI。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-tag-tree-graph-enhancement/08-CONTEXT.md
@.planning/phases/08-tag-tree-graph-enhancement/08-RESEARCH.md
@backend-go/internal/domain/topicanalysis/abstract_tag_service.go
@backend-go/internal/domain/topicanalysis/handler.go
@front/app/api/abstractTags.ts
@front/app/features/topic-graph/components/TagHierarchy.vue
@front/app/features/topic-graph/components/TagHierarchyRow.vue
</context>

<interfaces>
<!-- Key contracts from codebase -->

From backend-go/internal/domain/topicanalysis/abstract_tag_service.go:
```go
// Existing: DetachChildTag(parentID, childID uint) error
// Needed: ReassignTagParent(tagID, newParentID uint) error — move tag to new parent
```

From backend-go/internal/domain/topicanalysis/handler.go:
```go
// Handler pattern: gin.Context → validate → service call → JSON response
// Existing endpoints: GET /topic-tags/hierarchy, POST /topic-tags/:id/detach
```

From front/app/api/abstractTags.ts:
```typescript
export function useAbstractTagApi() {
  return {
    fetchHierarchy(...),
    updateAbstractName(tagId, newName),
    detachChild(parentId, childId),
    // Needed: reassignParent(tagId, newParentId)
  }
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 后端节点归类 API</name>
  <read_first>
    backend-go/internal/domain/topicanalysis/abstract_tag_service.go
    backend-go/internal/domain/topicanalysis/handler.go
    backend-go/internal/app/router.go
  </read_first>
  <behavior>
    - ReassignTagParent(tagID, newParentID) 从旧父节点 detach，添加到新父节点
    - 如果 tagID 当前不在任何抽象层级中（是独立标签），直接添加为 newParentID 的子节点
    - 如果 tagID 本身是抽象标签（有子节点），不允许归类到其他抽象标签（防止层级嵌套混乱）
    - POST /topic-tags/:id/reassign 接受 {parent_id: number}
  </behavior>
  <files>
    backend-go/internal/domain/topicanalysis/abstract_tag_service.go
    backend-go/internal/domain/topicanalysis/handler.go
    backend-go/internal/app/router.go
  </files>
  <action>
    1. 在 `abstract_tag_service.go` 中添加 `ReassignTagParent` 函数：
       ```go
       func ReassignTagParent(tagID, newParentID uint) error {
           if tagID == 0 || newParentID == 0 {
               return fmt.Errorf("tag_id and new_parent_id must be > 0")
           }
           if tagID == newParentID {
               return fmt.Errorf("tag_id and new_parent_id must be different")
           }

           return database.DB.Transaction(func(tx *gorm.DB) error {
               // 1. Remove from current parent (if any)
               tx.Where("child_id = ?", tagID).Delete(&models.TopicTagRelation{})

               // 2. Check if new parent is an abstract tag
               var parent models.TopicTag
               if err := tx.First(&parent, newParentID).Error; err != nil {
                   return fmt.Errorf("parent tag not found: %w", err)
               }

               // 3. Check if tag is itself an abstract tag (has children)
               var childCount int64
               tx.Model(&models.TopicTagRelation{}).Where("parent_id = ?", tagID).Count(&childCount)
               if childCount > 0 {
                   return fmt.Errorf("cannot reassign an abstract tag that has children")
               }

               // 4. Create new relation
               relation := models.TopicTagRelation{
                   ParentID:     newParentID,
                   ChildID:      tagID,
                   RelationType: "abstract",
               }
               return tx.Create(&relation).Error
           })
       }
       ```

    2. 在 `handler.go` 中添加 handler：
       ```go
       func ReassignTagHandler(c *gin.Context) {
           tagID, err := strconv.ParseUint(c.Param("id"), 10, 64)
           if err != nil {
               c.JSON(400, gin.H{"success": false, "error": "invalid tag id"})
               return
           }
           var req struct {
               ParentID uint `json:"parent_id"`
           }
           if err := c.ShouldBindJSON(&req); err != nil {
               c.JSON(400, gin.H{"success": false, "error": err.Error()})
               return
           }
           if err := ReassignTagParent(uint(tagID), req.ParentID); err != nil {
               c.JSON(400, gin.H{"success": false, "error": err.Error()})
               return
           }
           c.JSON(200, gin.H{"success": true, "message": "tag reassigned"})
       }
       ```

    3. 在 `router.go` 中注册路由：
       ```go
       topicTags.POST("/:id/reassign", topicanalysis.ReassignTagHandler)
       ```

    4. 添加 `strconv` import（如果不存在）。
  </action>
  <acceptance_criteria>
    - abstract_tag_service.go 包含 ReassignTagParent 函数
    - handler.go 包含 ReassignTagHandler
    - router.go 注册 POST /topic-tags/:id/reassign
    - `go test ./internal/domain/topicanalysis/... -v` 通过
    - `go build ./...` 通过
  </acceptance_criteria>
  <verify>
    <automated>cd backend-go && go test ./internal/domain/topicanalysis/... -v && go build ./...</automated>
  </verify>
  <done>后端提供节点归类 API，支持从旧父节点移除并添加到新父节点</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 前端节点归类弹窗 UI</name>
  <read_first>
    front/app/api/abstractTags.ts
    front/app/features/topic-graph/components/TagHierarchy.vue
    front/app/features/topic-graph/components/TagHierarchyRow.vue
  </read_first>
  <behavior>
    - TagHierarchyRow 提供操作入口（按钮或右键菜单）
    - 点击"归类"弹出 ReassignTagModal
    - Modal 显示当前标签信息 + embedding 相近的抽象标签列表
    - 选择目标抽象标签后调用 API 执行归类
    - 归类成功后刷新标签树
  </behavior>
  <files>
    front/app/api/abstractTags.ts
    front/app/features/topic-graph/components/TagHierarchyRow.vue
    front/app/features/topic-graph/components/TagHierarchy.vue
  </files>
  <action>
    1. 在 `front/app/api/abstractTags.ts` 中添加 reassign API：
       ```typescript
       async reassignTag(tagId: number, newParentId: number): Promise<ApiResponse<{ message: string }>> {
         return apiClient.post(`/topic-tags/${tagId}/reassign`, { parent_id: newParentId })
       }
       ```

    2. 创建 `ReassignTagModal.vue` 组件（或在 TagHierarchy.vue 中内联）：
       - Props: `tagId`, `tagLabel`, `visible`
       - Emits: `close`, `reassigned`
       - 内容：
         - 显示当前标签名
         - 加载相似的抽象标签列表（通过 fetchHierarchy 获取所有抽象标签，客户端按 embedding 相似度排序或复用搜索 API）
         - 列表项：标签名 + 类别 + 相似度
         - 选择后调用 `reassignTag(tagId, selectedParentId)`
         - 成功后 emit `reassigned` 并关闭

    3. 在 `TagHierarchyRow.vue` 中添加操作按钮：
       - 在标签名旁边添加一个 "⋮" 菜单按钮
       - 菜单包含"归类到其他抽象层"选项
       - 点击后打开 ReassignTagModal
       - 使用 `editingNodeId` 类似的状态管理

    4. 在 `TagHierarchy.vue` 中：
       - 添加 `reassignModal` state
       - 监听 `reassigned` 事件后调用 `loadHierarchy()` 刷新
  </action>
  <acceptance_criteria>
    - abstractTags.ts 包含 reassignTag API 函数
    - TagHierarchyRow 有归类操作入口
    - 弹窗显示可选的抽象标签列表
    - 选择后调用 API 执行归类
    - 归类成功后标签树刷新
    - `pnpm exec nuxi typecheck` 通过
    - `pnpm build` 通过
  </acceptance_criteria>
  <verify>
    <automated>cd front && pnpm exec nuxi typecheck && pnpm build</automated>
  </verify>
  <done>标签树节点可通过弹窗手动归类到其他抽象层</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI→API | tag_id 和 parent_id 来自前端 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-08-07 | Tampering | 伪造 parent_id | mitigate | 验证 parent_id 存在且为抽象标签 |
| T-08-08 | Denial of Service | 循环归类 | mitigate | 不允许抽象标签归类到其他抽象标签 |
</threat_model>

<verification>
1. go build 通过
2. go test 通过
3. pnpm exec nuxi typecheck 通过
4. pnpm build 通过
5. POST /topic-tags/:id/reassign 可用
</verification>

<success_criteria>
- 后端 ReassignTagParent 函数实现
- 后端 POST /topic-tags/:id/reassign 注册
- 前端归类弹窗 UI 实现
- 选择目标抽象标签后归类成功
- 归类后标签树刷新
</success_criteria>

<output>
After completion, create `.planning/phases/08-tag-tree-graph-enhancement/08-06-SUMMARY.md`
</output>
