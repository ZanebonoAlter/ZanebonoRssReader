---
phase: 08-tag-tree-graph-enhancement
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - backend-go/internal/domain/topicanalysis/abstract_tag_service.go
autonomous: true
requirements: []

must_haves:
  truths:
    - "抽象标签提取时 LLM 同时返回 name 和 description"
    - "抽象标签的 description 基于子标签 description 聚合总结"
    - "description 字段在抽象标签创建时即写入数据库"
  artifacts:
    - path: "backend-go/internal/domain/topicanalysis/abstract_tag_service.go"
      provides: "callLLMForAbstractName 返回 name+description, buildAbstractTagPrompt 包含子标签 description"
      exports: ["ExtractAbstractTag"]
  key_links:
    - from: "abstract_tag_service.go:callLLMForAbstractName()"
      to: "TopicTag.Description"
      via: "扩展 JSON 响应解析"
      pattern: "result.Description = parsed.Description"
---

<objective>
**Plan 02: 抽象标签 Description 生成（扩展 ExtractAbstractTag LLM 调用）**

Purpose: 扩展现有的 callLLMForAbstractName 函数，使其在生成抽象标签名称的同时返回 description（D-02）。description 基于子标签的 description 聚合总结。
Output: callLLMForAbstractName 返回 name+description，ExtractAbstractTag 设置 TopicTag.Description。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-tag-tree-graph-enhancement/08-CONTEXT.md
@.planning/phases/08-tag-tree-graph-enhancement/08-RESEARCH.md
@backend-go/internal/domain/topicanalysis/abstract_tag_service.go
</context>

<interfaces>
<!-- Key contracts from codebase -->

From backend-go/internal/domain/topicanalysis/abstract_tag_service.go:
```go
// callLLMForAbstractName — current pattern
func callLLMForAbstractName(ctx context.Context, candidates []TagCandidate, newLabel string) (string, error) {
    router := airouter.NewRouter()
    prompt := buildAbstractTagPrompt(candidates, newLabel)
    req := airouter.ChatRequest{
        Capability:  airouter.CapabilityTopicTagging,
        Messages:    []airouter.Message{{Role: "system", Content: "..."}, {Role: "user", Content: prompt}},
        JSONMode:    true,
        Temperature: func() *float64 { f := 0.3; return &f }(),
    }
    result, _ := router.Chat(ctx, req)
    return parseAbstractNameFromJSON(result.Content)
}

// Current JSON response format: {"abstract_name": "...", "reason": "..."}
// Needed: {"abstract_name": "...", "description": "...", "reason": "..."}
```

From backend-go/internal/domain/models/topic_graph.go:
```go
type TopicTag struct {
    // ... existing fields
    Description string `gorm:"type:text" json:"description"` // Added in Plan 01
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 扩展 callLLMForAbstractName 返回 description</name>
  <read_first>
    backend-go/internal/domain/topicanalysis/abstract_tag_service.go
  </read_first>
  <behavior>
    - buildAbstractTagPrompt 包含子标签的 description（如果有的话）
    - LLM JSON 响应格式变为 {"abstract_name": "...", "description": "...", "reason": "..."}
    - callLLMForAbstractName 返回 (name string, description string, err error)
    - ExtractAbstractTag 创建抽象标签时设置 Description 字段
    - 子标签没有 description 时，prompt 仅包含 label（降级但不失败）
  </behavior>
  <files>
    backend-go/internal/domain/topicanalysis/abstract_tag_service.go
  </files>
  <action>
    1. 修改 `callLLMForAbstractName` 签名和返回值：
       ```go
       func callLLMForAbstractName(ctx context.Context, candidates []TagCandidate, newLabel string) (string, string, error) {
       ```
       返回 `(abstractName, description string, err error)`。

    2. 修改 `buildAbstractTagPrompt`，在候选标签信息中包含 description：
       ```go
       func buildAbstractTagPrompt(candidates []TagCandidate, newLabel string) string {
           var parts []string
           for _, c := range candidates {
               if c.Tag != nil {
                   desc := ""
                   if c.Tag.Description != "" {
                       desc = fmt.Sprintf(" (description: %s)", c.Tag.Description)
                   }
                   parts = append(parts, fmt.Sprintf("- %q (similarity: %.2f)%s", c.Tag.Label, c.Similarity, desc))
               }
           }
           parts = append(parts, fmt.Sprintf("- %q (new tag)", newLabel))

           return fmt.Sprintf(`Given these semantically similar tags:
   %s

   Extract a common abstract concept that encompasses ALL of them.
   The abstract name should be broader and more general.
   Also generate a brief description (1-2 sentences) summarizing the common theme.

   Respond with JSON:
   {"abstract_name": "your answer", "description": "brief description", "reason": "explanation"}

   Rules:
   - abstract_name must be 1-160 characters
   - description must be 1-500 characters
   - abstract_name should be in the original language of the tags
   - Prefer concise, meaningful names over vague descriptions`,
               strings.Join(parts, "\n"))
       }
       ```

    3. 修改 `parseAbstractNameFromJSON`（或新建 `parseAbstractTagResponse`）返回 name + description：
       ```go
       func parseAbstractTagResponse(content string) (string, string, error) {
           content = strings.TrimSpace(content)
           var result struct {
               AbstractName string `json:"abstract_name"`
               Description  string `json:"description"`
               Reason       string `json:"reason"`
           }
           if err := json.Unmarshal([]byte(content), &result); err != nil {
               return "", "", fmt.Errorf("failed to parse LLM response: %w", err)
           }
           name := strings.TrimSpace(result.AbstractName)
           if name == "" {
               return "", "", fmt.Errorf("LLM returned empty abstract_name")
           }
           if len(name) > maxAbstractNameLen {
               return "", "", fmt.Errorf("abstract_name exceeds %d characters", maxAbstractNameLen)
           }
           desc := strings.TrimSpace(result.Description)
           if len(desc) > 500 {
               desc = desc[:500]
           }
           return name, desc, nil
       }
       ```

    4. 更新 `ExtractAbstractTag` 中的调用：
       ```go
       abstractName, abstractDesc, err := callLLMForAbstractName(ctx, candidates, newLabel)
       ```
       在创建 abstractTag 时设置 Description：
       ```go
       abstractTag = &models.TopicTag{
           Slug: slug, Label: abstractName, Category: category,
           Source: "abstract", Status: "active",
           Description: abstractDesc,
       }
       ```

    5. 保留旧的 `parseAbstractNameFromJSON` 和 `callLLMForAbstractName` 旧签名作为兼容（如果有其他调用方），或全部替换。
  </action>
  <acceptance_criteria>
    - buildAbstractTagPrompt 包含子标签 description
    - JSON 响应格式包含 description 字段
    - ExtractAbstractTag 创建的抽象标签带有 description
    - `go test ./internal/domain/topicanalysis/... -v` 通过
    - `go build ./...` 通过
  </acceptance_criteria>
  <verify>
    <automated>cd backend-go && go test ./internal/domain/topicanalysis/... -v</automated>
  </verify>
  <done>抽象标签提取时同时生成 description，写入数据库</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API→LLM | LLM response 被解析为 description |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-08-03 | Tampering | LLM description 超长 | mitigate | 截断到 500 字符 |
</threat_model>

<verification>
1. go build 通过
2. go test 通过
3. callLLMForAbstractName 返回 (name, description, err)
4. buildAbstractTagPrompt 包含子标签 description
</verification>

<success_criteria>
- callLLMForAbstractName 扩展为返回 name + description
- buildAbstractTagPrompt 包含子标签 description 信息
- ExtractAbstractTag 创建抽象标签时设置 Description
- 降级路径：子标签无 description 时不失败
</success_criteria>

<output>
After completion, create `.planning/phases/08-tag-tree-graph-enhancement/08-02-SUMMARY.md`
</output>
