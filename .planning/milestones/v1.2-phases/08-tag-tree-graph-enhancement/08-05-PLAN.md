---
phase: 08-tag-tree-graph-enhancement
plan: 05
type: execute
wave: 4
depends_on:
  - 08-04
files_modified:
  - front/app/features/topic-graph/pages/TopicGraphPage.vue
  - front/app/components/dialog/GlobalSettingsDialog.vue
  - front/app/features/ai/components/MergeReembeddingQueuePanel.vue
autonomous: true
requirements: []

must_haves:
  truths:
    - "TopicGraphPage 不再挂载 TagMergePreview"
    - "GlobalSettingsDialog 新增 tag-merge tab，TagMergePreview 挂载其中"
    - "合并完成后提示用户手动触发抽象层关系重建"
    - "提示不阻断用户操作流程（toast 或 inline 提示）"
  artifacts:
    - path: "front/app/features/topic-graph/pages/TopicGraphPage.vue"
      provides: "移除 TagMergePreview 引用和挂载"
    - path: "front/app/components/dialog/GlobalSettingsDialog.vue"
      provides: "新增 tag-merge tab，挂载 TagMergePreview + 重建提示逻辑"
  key_links:
    - from: "TopicGraphPage.vue"
      to: "GlobalSettingsDialog.vue"
      via: "TagMergePreview 组件迁移"
      pattern: "TagMergePreview.*import"
    - from: "TagMergePreview @merged event"
      to: "rebuild prompt"
      via: "toast 提示用户触发重建"
      pattern: "merged.*summary"
---

<objective>
**Plan 05: TagMergePreview 迁移至设置页 + 合并后重建提示**

Purpose: 将 TagMergePreview 从 TopicGraphPage 迁移到 GlobalSettingsDialog（D-07），并在合并完成后提示用户手动触发抽象层关系重建（D-08）。
Output: TopicGraphPage 移除 TagMergePreview + GlobalSettingsDialog 新增 tag-merge tab + 合并后 toast 提示。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-tag-tree-graph-enhancement/08-CONTEXT.md
@.planning/phases/08-tag-tree-graph-enhancement/08-RESEARCH.md
@front/app/features/topic-graph/pages/TopicGraphPage.vue
@front/app/components/dialog/GlobalSettingsDialog.vue
@front/app/features/topic-graph/components/TagMergePreview.vue
</context>

<interfaces>
<!-- Key contracts from codebase -->

From front/app/features/topic-graph/components/TagMergePreview.vue:
```typescript
interface Props {
  visible: boolean
  scopeCategoryId?: string | null
  scopeFeedId?: string | null
}
const emit = defineEmits<{
  close: []
  merged: [summary: MergeSummary]
}>()
```

From front/app/components/dialog/GlobalSettingsDialog.vue:
```typescript
// Tab system
const activeTab = ref<'feeds' | 'categories' | 'general' | 'backend-queues' | 'preferences' | 'firecrawl' | 'schedulers'>('feeds')

// Already imports: MergeReembeddingQueuePanel from '~/features/ai/components/MergeReembeddingQueuePanel.vue'
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TagMergePreview 迁移至 GlobalSettingsDialog</name>
  <read_first>
    front/app/features/topic-graph/pages/TopicGraphPage.vue
    front/app/components/dialog/GlobalSettingsDialog.vue
    front/app/features/topic-graph/components/TagMergePreview.vue
  </read_first>
  <behavior>
    - TopicGraphPage 中移除 TagMergePreview 的 import 和挂载
    - GlobalSettingsDialog 中新增 'tag-merge' tab
    - TagMergePreview 挂载在 tag-merge tab 中（在 merge 队列旁边）
    - 合并完成后通过 toast 提示用户重建抽象层
    - 提示不阻断操作（非 modal）
  </behavior>
  <files>
    front/app/features/topic-graph/pages/TopicGraphPage.vue
    front/app/components/dialog/GlobalSettingsDialog.vue
  </files>
  <action>
    1. 在 `TopicGraphPage.vue` 中：
       - 移除 TagMergePreview 的 import 语句
       - 移除 TagMergePreview 相关的 ref/state（如 showMergePreview）
       - 移除 `<TagMergePreview>` 组件挂载
       - 移除触发合并预览的按钮（如果有）

    2. 在 `GlobalSettingsDialog.vue` 中：
       - 添加 import：
         ```typescript
         import TagMergePreview from '~/features/topic-graph/components/TagMergePreview.vue'
         ```
       - 扩展 activeTab 类型，添加 `'tag-merge'`：
         ```typescript
         const activeTab = ref<'feeds' | 'categories' | 'general' | 'backend-queues' | 'preferences' | 'firecrawl' | 'schedulers' | 'tag-merge'>('feeds')
         ```
       - 在 tab 按钮区域添加 "标签合并" tab（放在 backend-queues 旁边）。
       - 在 tab content 区域添加 tag-merge 面板：
         ```html
         <div v-if="activeTab === 'tag-merge'" class="space-y-4">
           <TagMergePreview
             :visible="true"
             @merged="handleMerged"
             @close="activeTab = 'backend-queues'"
           />
         </div>
         ```
       - 添加 `handleMerged` 函数，使用 toast 提示用户：
         ```typescript
         function handleMerged(summary: MergeSummary) {
           // Show non-blocking toast
           showToast('标签合并完成。合并可能影响抽象层结构，建议在设置中重建抽象层关系。', 'info')
         }
         ```
       - 如果项目使用 sonner 或其他 toast 库，使用对应 API。
       - 如果没有现成 toast 库，使用简单的 inline 状态提示。
  </action>
  <acceptance_criteria>
    - TopicGraphPage 不再 import 或使用 TagMergePreview
    - GlobalSettingsDialog 新增 tag-merge tab
    - TagMergePreview 在 tag-merge tab 中显示
    - 合并完成后显示 toast 提示
    - `pnpm exec nuxi typecheck` 通过
    - `pnpm build` 通过
  </acceptance_criteria>
  <verify>
    <automated>cd front && pnpm exec nuxi typecheck && pnpm build</automated>
  </verify>
  <done>TagMergePreview 迁移到设置页，合并后有重建提示</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI→API | 合并操作通过 API 执行 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-08-06 | Denial of Service | 误触发批量合并 | accept | TagMergePreview 已有确认流程 |
</threat_model>

<verification>
1. pnpm exec nuxi typecheck 通过
2. pnpm build 通过
3. TopicGraphPage 不含 TagMergePreview
4. GlobalSettingsDialog 包含 tag-merge tab
</verification>

<success_criteria>
- TagMergePreview 已从 TopicGraphPage 移除
- GlobalSettingsDialog 新增 tag-merge tab
- TagMergePreview 在 tag-merge tab 中可正常使用
- 合并完成后用户看到重建提示（非阻断式）
</success_criteria>

<output>
After completion, create `.planning/phases/08-tag-tree-graph-enhancement/08-05-SUMMARY.md`
</output>
