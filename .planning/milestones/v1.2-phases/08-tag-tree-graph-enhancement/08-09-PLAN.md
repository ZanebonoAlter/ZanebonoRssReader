---
phase: 08-tag-tree-graph-enhancement
plan: 09
type: execute
wave: 2
depends_on: []
files_modified:
  - front/app/features/topic-graph/components/TagHierarchy.vue
  - front/app/features/topic-graph/components/TagHierarchyRow.vue
autonomous: true
gap_closure: true
requirements: []

must_haves:
  truths:
    - "TagHierarchy has custom date range picker beyond 7d/30d/全部"
    - "Inactive tags sort BELOW active tags in the tree, not just dimmed"
  artifacts:
    - path: "front/app/features/topic-graph/components/TagHierarchy.vue"
      provides: "Custom date range picker UI"
      contains: "customRange\|dateRangePicker\|startDate"
    - path: "front/app/features/topic-graph/components/TagHierarchyRow.vue"
      provides: "Active tags render before inactive tags"
      contains: "sortOrder\|isActive"
  key_links:
    - from: "front/app/features/topic-graph/components/TagHierarchy.vue"
      to: "front/app/api/abstractTags.ts"
      via: "timeRange parameter"
      pattern: "timeRange"
---

<objective>
Add custom time range picker and fix inactive tag sorting in the tag hierarchy tree.

Purpose: User reported (Tests 4, 5) that the time range filter works but needs: (1) custom time range selection beyond preset 7d/30d/全部, and (2) inactive tags sorted BELOW active tags instead of just being dimmed.

Output: Tag hierarchy has a custom date range picker. Inactive tags appear after active tags in each level of the tree.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-tag-tree-graph-enhancement/08-UAT.md
@.planning/phases/08-tag-tree-graph-enhancement/08-03-SUMMARY.md

<interfaces>
<!-- Backend API — already supports timeRange parameter -->
From backend-go/internal/domain/topicanalysis/abstract_tag_handler.go:
- Handler reads `time_range` query param
- Accepts "7d", "30d", or empty (no filter)
- For custom ranges, backend needs to accept start_date/end_date params OR a custom range format like "custom:2026-04-01:2026-04-14"

From front/app/api/abstractTags.ts:
```typescript
async fetchHierarchy(
  category?: string,
  feedId?: string,
  categoryId?: string,
  showUnclassified?: boolean,
  timeRange?: string,
) {
  // ... passes timeRange to API
}
```

From front/app/features/topic-graph/components/TagHierarchy.vue:
- Time filter buttons at lines 293-320: 全部 / 7天 / 30天
- timeRange ref at line 21
- loadHierarchy passes timeRange to API at line 86
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add custom date range picker + inactive tag sorting to TagHierarchy</name>
  <files>
    front/app/features/topic-graph/components/TagHierarchy.vue,
    front/app/features/topic-graph/components/TagHierarchyRow.vue,
    backend-go/internal/domain/topicanalysis/abstract_tag_service.go,
    backend-go/internal/domain/topicanalysis/abstract_tag_handler.go
  </files>
  <read_first>
    front/app/features/topic-graph/components/TagHierarchy.vue,
    front/app/features/topic-graph/components/TagHierarchyRow.vue,
    backend-go/internal/domain/topicanalysis/abstract_tag_service.go,
    backend-go/internal/domain/topicanalysis/abstract_tag_handler.go
  </read_first>
  <action>
**Part A — Backend: Support custom date range in time_range parameter**

In `backend-go/internal/domain/topicanalysis/abstract_tag_service.go`, find the `resolveActiveTagIDs` function. Currently it accepts `timeRange string` and handles "7d" and "30d". Extend it to also handle custom ranges in the format `"custom:YYYY-MM-DD:YYYY-MM-DD"`:

```go
case strings.HasPrefix(timeRange, "custom:"):
    parts := strings.SplitN(timeRange, ":", 3)
    if len(parts) == 3 {
        startDate := parts[1]
        endDate := parts[2]
        query = query.Where("articles.pub_date >= ? AND articles.pub_date <= ?", startDate+" 00:00:00", endDate+" 23:59:59")
    }
```

In `backend-go/internal/domain/topicanalysis/abstract_tag_handler.go`, ensure the `time_range` param is passed through as-is — no change needed since it already reads `c.Query("time_range")`.

**Part B — Frontend: Add custom date range picker UI**

In `front/app/features/topic-graph/components/TagHierarchy.vue`:

1. Add reactive state for custom range:
   ```typescript
   const customStartDate = ref('')
   const customEndDate = ref('')
   const showCustomRange = ref(false)
   ```

2. Replace the time filter section (lines 293-320) with an enhanced version that includes a "自定义" button and date inputs:

   ```html
   <div class="flex items-center gap-1.5 mb-3 flex-wrap">
     <span class="text-xs text-white/40 mr-0.5">时间</span>
     <button type="button" class="th-category-btn" :class="{ 'th-category-btn--active': timeRange === '' }" @click="timeRange = ''">全部</button>
     <button type="button" class="th-category-btn" :class="{ 'th-category-btn--active': timeRange === '7d' }" @click="timeRange = '7d'">7天</button>
     <button type="button" class="th-category-btn" :class="{ 'th-category-btn--active': timeRange === '30d' }" @click="timeRange = '30d'">30天</button>
     <button type="button" class="th-category-btn" :class="{ 'th-category-btn--active': showCustomRange }" @click="showCustomRange = !showCustomRange">
       <Icon icon="mdi:calendar-range" width="12" class="mr-1" />
       自定义
     </button>
   </div>
   <div v-if="showCustomRange" class="flex items-center gap-2 mb-3 th-custom-range">
     <input v-model="customStartDate" type="date" class="th-date-input" />
     <span class="text-white/30 text-xs">至</span>
     <input v-model="customEndDate" type="date" class="th-date-input" />
     <button type="button" class="th-category-btn th-category-btn--active" @click="applyCustomRange">确定</button>
   </div>
   ```

3. Add the `applyCustomRange` function:
   ```typescript
   function applyCustomRange() {
     if (customStartDate.value && customEndDate.value) {
       timeRange.value = `custom:${customStartDate.value}:${customEndDate.value}`
     }
   }
   ```

4. Update the `watch(timeRange, ...)` to also handle clearing custom state:
   ```typescript
   watch(timeRange, (newVal) => {
     if (!newVal.startsWith('custom:')) {
       showCustomRange.value = false
     }
     void loadHierarchy()
   })
   ```

5. Add CSS for the date inputs:
   ```css
   .th-date-input {
     border: 1px solid rgba(255, 255, 255, 0.1);
     border-radius: 8px;
     background: rgba(255, 255, 255, 0.04);
     color: rgba(255, 255, 255, 0.85);
     font-size: 0.72rem;
     padding: 0.25rem 0.5rem;
     outline: none;
   }
   .th-date-input:focus { border-color: rgba(240, 138, 75, 0.4); }
   .th-custom-range { margin-top: -0.25rem; }
   ```

**Part C — Frontend: Sort inactive tags below active tags**

In `front/app/features/topic-graph/components/TagHierarchy.vue`:

1. Add a sorting computed that reorders `filteredNodes` to put active before inactive at each level:
   ```typescript
   function sortNodesByActivity(list: TagHierarchyNode[]): TagHierarchyNode[] {
     return [...list].sort((a, b) => {
       // Active nodes come first (isActive=true sorts before false)
       const aActive = a.isActive !== false
       const bActive = b.isActive !== false
       if (aActive !== bActive) return aActive ? -1 : 1
       return 0
     }).map(node => ({
       ...node,
       children: sortNodesByActivity(node.children),
     }))
   }

   const sortedNodes = computed(() => sortNodesByActivity(filteredNodes.value))
   ```

2. Update the template to use `sortedNodes` instead of `filteredNodes` in the tree rendering:
   ```html
   <TagHierarchyRow
     v-for="node in sortedNodes"
     ...
   />
   ```

Run `pnpm exec nuxi typecheck` and `pnpm build`.
  </action>
  <acceptance_criteria>
    - TagHierarchy.vue has customStartDate and customEndDate refs
    - TagHierarchy.vue has "自定义" button that toggles date picker
    - applyCustomRange function creates "custom:YYYY-MM-DD:YYYY-MM-DD" format
    - Backend resolveActiveTagIDs handles "custom:" prefix
    - sortNodesByActivity function sorts inactive tags below active
    - Template uses sortedNodes instead of filteredNodes
    - `pnpm exec nuxi typecheck` passes
    - `pnpm build` passes
    - `go build ./...` passes
  </acceptance_criteria>
  <verify>
    <automated>cd backend-go && go build ./... && cd ../front && pnpm exec nuxi typecheck && pnpm build</automated>
  </verify>
  <done>Custom date range picker works in tag hierarchy. Inactive tags sorted below active tags at every tree level. All builds pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend → Backend API | Custom date range passed as string — backend validates format |
| Backend → DB | Date strings used in SQL WHERE — parameterized via GORM, no injection risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-09-01 | T | Custom date range parsing | mitigate | Validate date format YYYY-MM-DD before using in query, reject malformed input |
| T-08-09-02 | I | Sort order manipulation | accept | Sort is visual-only, no security impact |
</threat_model>

<verification>
1. `go build ./...` passes
2. `pnpm exec nuxi typecheck` passes
3. `pnpm build` passes
4. Custom date range picker appears in TagHierarchy time filter area
5. Clicking "自定义" shows start/end date inputs
6. Setting dates and clicking "确定" triggers hierarchy reload with custom range
7. Inactive tags appear after active tags in the tree
</verification>

<success_criteria>
- Custom date range picker functional in tag hierarchy
- Inactive tags sorted below active tags at every level
- Backend handles custom:date:date format
- All build commands pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-tag-tree-graph-enhancement/08-09-SUMMARY.md`
</output>
