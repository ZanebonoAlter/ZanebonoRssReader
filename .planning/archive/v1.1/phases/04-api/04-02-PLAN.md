---
phase: 04-api
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [
  "backend-go/internal/jobs/handler.go",
  "backend-go/internal/jobs/auto_refresh.go",
  "backend-go/internal/jobs/firecrawl.go",
  "backend-go/internal/jobs/content_completion.go",
  "backend-go/internal/jobs/auto_summary.go",
  "backend-go/internal/jobs/preference_update.go"
]
autonomous: true
requirements: ["API-04"]
user_setup: []

must_haves:
  truths:
    - "All scheduler status API endpoints return identical field structure"
    - "Each status response includes name, status, check_interval, next_run, is_executing fields"
    - "Field types are consistent across all scheduler status responses"
  artifacts:
    - path: "backend-go/internal/jobs/handler.go"
      provides: "Unified scheduler status response struct"
      contains: "type SchedulerStatusResponse struct { Name string; Status string; CheckInterval int64; NextRun int64; IsExecuting bool }"
    - path: "backend-go/internal/jobs/*_status.go"
      provides: "Consistent status response implementation for each scheduler"
      contains: "Returns SchedulerStatusResponse instead of custom structs"
  key_links:
    - from: "backend-go/internal/jobs/handler.go"
      to: "backend-go/internal/jobs/auto_refresh.go"
      via: "GetStatus() method returns SchedulerStatusResponse"
    - from: "backend-go/internal/jobs/handler.go"
      to: "backend-go/internal/jobs/firecrawl.go"
      via: "GetStatus() method returns SchedulerStatusResponse"
---

<objective>
Standardize all scheduler status API response formats to be consistent across all endpoints.

Purpose: Ensure frontend can consume scheduler status responses with a single parsing logic, no custom handling per scheduler.
Output: All scheduler status endpoints return identical field structure.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@backend-go/internal/jobs/handler.go
@backend-go/internal/jobs/auto_refresh.go
@backend-go/internal/jobs/firecrawl.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define unified SchedulerStatusResponse struct</name>
  <files>backend-go/internal/jobs/handler.go</files>
  <behavior>
    - Test: Struct contains all required fields: Name (string), Status (string), CheckInterval (int64, seconds), NextRun (int64, Unix timestamp), IsExecuting (bool)
    - Test: JSON struct tags use snake_case for all fields
  </behavior>
  <action>
    1. Add `type SchedulerStatusResponse struct` definition in handler.go
    2. Add JSON tags for all fields to ensure snake_case output:
       - Name `json:"name"`
       - Status `json:"status"`
       - CheckInterval `json:"check_interval"`
       - NextRun `json:"next_run"`
       - IsExecuting `json:"is_executing"`
  </action>
  <verify>
    <automated>cd backend-go && go build ./internal/jobs</automated>
  </verify>
  <done>SchedulerStatusResponse struct is defined with all required fields and correct JSON tags.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update all scheduler GetStatus() methods to return unified struct</name>
  <files>
    backend-go/internal/jobs/auto_refresh.go,
    backend-go/internal/jobs/firecrawl.go,
    backend-go/internal/jobs/content_completion.go,
    backend-go/internal/jobs/auto_summary.go,
    backend-go/internal/jobs/preference_update.go
  </files>
  <behavior>
    - Test: Each scheduler's GetStatus() method populates all fields of SchedulerStatusResponse correctly
    - Test: No missing fields in any scheduler's status response
    - Test: Field types match the struct definition exactly
  </behavior>
  <action>
    1. For each scheduler implementation:
       - Update GetStatus() method signature to return SchedulerStatusResponse
       - Map existing status values to the new struct fields:
         - Name: Scheduler display name (e.g. "Auto Refresh", "Firecrawl Crawler")
         - Status: Current status string ("running", "idle", "error")
         - CheckInterval: Interval in seconds between runs
         - NextRun: Unix timestamp of next scheduled run, 0 if no next run
         - IsExecuting: Boolean indicating if scheduler is currently running a job
    2. Ensure all fields are populated, no zero values unless intentional
  </action>
  <verify>
    <automated>cd backend-go && go test ./internal/jobs -v -run TestSchedulerStatusFormat</automated>
  </verify>
  <done>All scheduler GetStatus() methods return the unified SchedulerStatusResponse struct with all fields populated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update API handlers to return unified response</name>
  <files>backend-go/internal/jobs/handler.go</files>
  <behavior>
    - Test: All /api/jobs/*/status endpoints return SchedulerStatusResponse JSON
    - Test: Response format is identical across all scheduler status endpoints
  </behavior>
  <action>
    1. Update each status endpoint handler to call the scheduler's GetStatus() method
    2. Return the struct directly as `gin.H{"success": true, "data": statusResponse}`
    3. Remove any custom response formatting for individual scheduler status endpoints
  </action>
  <verify>
    <automated>cd backend-go && go run cmd/server/main.go & curl http://localhost:5000/api/jobs/auto-refresh/status | grep -q '"name":"Auto Refresh"' && curl http://localhost:5000/api/jobs/firecrawl/status | grep -q '"name":"Firecrawl Crawler"'</automated>
  </verify>
  <done>All scheduler status API endpoints return identical JSON structure with all required fields.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Backend API → Frontend | Response format changes can break existing frontend parsing |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Tampering | Response field mismatch | Mitigate | Add unit tests to verify all status endpoints return identical structure |
| T-04-04 | Information Disclosure | Accidental exposure of internal scheduler state | Accept | All fields are already public information |
</threat_model>

<verification>
1. Run `go build ./internal/jobs` to confirm no compilation errors
2. Run `go test ./internal/jobs/...` to confirm all unit tests pass
3. Manual verification:
   - Call GET /api/jobs/auto-refresh/status, confirm all fields exist
   - Call GET /api/jobs/firecrawl/status, confirm same field structure
   - Call GET /api/jobs/content-completion/status, confirm same field structure
   - Call GET /api/jobs/auto-summary/status, confirm same field structure
   - Call GET /api/jobs/preference-update/status, confirm same field structure
</verification>

<success_criteria>
1. ✅ All scheduler status API endpoints return identical field structure
2. ✅ Each response contains: name, status, check_interval, next_run, is_executing fields
3. ✅ Field types are consistent across all responses (string, string, int, int, boolean)
</success_criteria>

<output>
After completion, create `.planning/phases/04-api/04-02-SUMMARY.md`
</output>
