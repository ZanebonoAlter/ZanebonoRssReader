---
phase: 04-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "front/app/api/scheduler.ts",
  "front/app/stores/api.ts",
  "front/app/features/articles/components/ArticleContentActions.vue"
]
autonomous: true
requirements: ["API-01", "API-02", "API-03"]
user_setup: []

must_haves:
  truths:
    - "Scheduler trigger API calls use apiClient instead of raw fetch"
    - "Marking article as read updates feed unread count correctly"
    - "Mark all as read clears unread count for all feeds including uncategorized"
  artifacts:
    - path: "front/app/api/scheduler.ts"
      provides: "Unified scheduler API client implementation"
      contains: "apiClient.post calls instead of fetch"
    - path: "front/app/stores/api.ts"
      provides: "Unread count refresh after article update"
      contains: "updateUnreadCount() call after markAsRead/markAllAsRead"
  key_links:
    - from: "front/app/api/scheduler.ts"
      to: "front/app/api/client.ts"
      via: "import { apiClient } from '~/api/client'"
    - from: "front/app/stores/api.ts"
      to: "front/app/features/articles/components/ArticleContentActions.vue"
      via: "markAsRead() method calls store update"
---

<objective>
Fix frontend API usage inconsistencies and ensure correct state synchronization.

Purpose: Resolve API call inconsistencies between raw fetch and apiClient, fix unread count drift after article updates, and ensure mark all as read works for all feed categories.
Output: Frontend API calls standardized, state synchronization correct.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@front/app/api/client.ts
@front/app/stores/api.ts
@front/app/api/scheduler.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace raw fetch with apiClient in scheduler API (API-01)</name>
  <files>front/app/api/scheduler.ts</files>
  <behavior>
    - Test: All scheduler trigger functions use apiClient instead of global fetch
    - Test: API responses are parsed consistently with ApiResponse<T> type
  </behavior>
  <action>
    1. Import `apiClient` from `~/api/client` at the top of scheduler.ts
    2. Replace all `fetch` calls with `apiClient.post`/`apiClient.get`
    3. Ensure response handling follows existing ApiResponse pattern (success, data, error, message)
    4. Update return types to match actual API response structure
  </action>
  <verify>
    <automated>pnpm exec nuxi typecheck front/app/api/scheduler.ts</automated>
  </verify>
  <done>scheduler.ts no longer contains `fetch` calls, all API requests use apiClient, typecheck passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refresh unreadCount after article update (API-02)</name>
  <files>front/app/stores/api.ts</files>
  <behavior>
    - Test: After `updateArticle` (mark as read) returns successfully, `unreadCount` for the article's feed is refreshed
    - Test: Unread count decreases by exactly 1 when an article is marked as read
  </behavior>
  <action>
    1. In the `updateArticle` action, after successful API response, extract the article's `feed_id`
    2. Call `getFeedUnreadCount(feed_id)` to refresh the feed's unread count
    3. Ensure the UI state updates immediately without full page reload
  </action>
  <verify>
    <automated>pnpm test:unit -- app/stores/api.test.ts -t "updateArticle refreshes unread count"</automated>
  </verify>
  <done>Marking an article as read causes the corresponding feed's unread count to decrease by 1 in the sidebar.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fix markAllAsRead to cover all feeds including uncategorized (API-03)</name>
  <files>front/app/stores/api.ts</files>
  <behavior>
    - Test: `markAllAsRead` action triggers unread count refresh for all feeds, not just categorized ones
    - Test: Uncategorized feed unread count is also cleared after mark all as read
  </behavior>
  <action>
    1. In the `markAllAsRead` action, after successful API response, call `getAllFeedUnreadCounts()` instead of only refreshing specific feeds
    2. Ensure uncategorized feed (feed_id=0 or null) is included in the refresh
    3. Verify all feed unread counts show 0 after operation completes
  </action>
  <verify>
    <automated>pnpm test:unit -- app/stores/api.test.ts -t "markAllAsRead refreshes all feed unread counts"</automated>
  </verify>
  <done>Clicking "全部标记已读" clears unread count for all feeds including the uncategorized feed in the sidebar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Frontend → Backend API | Untrusted network requests, response parsing can fail |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | API response parsing | Mitigate | Use apiClient's built-in response validation and error handling |
| T-04-02 | Denial of Service | Unread count refresh loops | Accept | Low risk, only 1 API call per mark as read operation |
</threat_model>

<verification>
1. Run `pnpm exec nuxi typecheck` to confirm no TypeScript errors
2. Run `pnpm test:unit` to confirm all unit tests pass
3. Manual verification:
   - Go to scheduler page, click trigger buttons, confirm requests use apiClient (check network tab)
   - Mark an article as read, confirm feed unread count decreases by 1
   - Click "全部标记已读", confirm all feed unread counts show 0
</verification>

<success_criteria>
1. ✅ scheduler.ts uses apiClient exclusively for all API calls (no fetch calls remaining)
2. ✅ Marking article as read updates corresponding feed unread count correctly
3. ✅ "全部标记已读" clears unread count for all feeds including uncategorized
</success_criteria>

<output>
After completion, create `.planning/phases/04-api/04-01-SUMMARY.md`
</output>
