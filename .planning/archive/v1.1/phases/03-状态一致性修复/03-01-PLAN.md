---
phase: 03-状态一致性修复
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [backend-go/internal/domain/feeds/service.go]
autonomous: true
requirements: [STAT-03]

must_haves:
  truths:
    - "创建只开启ArticleSummaryEnabled不开启Firecrawl的feed，新文章summary_status为pending而非complete"
    - "开启Firecrawl的feed，新文章summary_status仍为incomplete（现有逻辑不变）"
    - "两个开关都关闭的feed，新文章summary_status仍为complete（现有逻辑不变）"
  artifacts:
    - path: "backend-go/internal/domain/feeds/service.go"
      provides: "buildArticleFromEntry函数状态初始化逻辑修正"
      contains: "if feed.ArticleSummaryEnabled && !feed.FirecrawlEnabled"
  key_links:
    - from: "feeds/service.go:buildArticleFromEntry"
      to: "models.Article.SummaryStatus"
      via: "状态赋值"
      pattern: "article.SummaryStatus.*=.*pending"
---

<objective>
修正buildArticleFromEntry函数，当feed只开启ArticleSummaryEnabled不开启Firecrawl时，文章summary_status初始化为"pending"而非默认的"complete"。

Purpose: 解决STAT-03需求，确保文章初始状态正确反映feed配置，避免只开启摘要功能的feed文章被误标记为已完成。
Output: feeds/service.go中的buildArticleFromEntry函数逻辑修正。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-状态一致性修复/03-CONTEXT.md

<interfaces>
<!-- Key interfaces executor needs -->

From backend-go/internal/domain/models/article.go:
```go
type Article struct {
    SummaryStatus string `gorm:"size:20;default:complete" json:"summary_status"`
    // ... other fields
}
```

From backend-go/internal/domain/models/feed.go (via feeds/service.go usage):
```go
type Feed struct {
    FirecrawlEnabled      bool `gorm:"default:false" json:"firecrawl_enabled"`
    ArticleSummaryEnabled bool `gorm:"default:false" json:"article_summary_enabled"`
    // ... other fields
}
```

Existing buildArticleFromEntry logic (lines 173-194):
```go
func (s *FeedService) buildArticleFromEntry(feed models.Feed, entry ParsedEntry) models.Article {
    article := models.Article{
        // ... fields
        SummaryStatus: "complete",  // Default value
    }

    if feed.FirecrawlEnabled {
        article.FirecrawlStatus = "pending"
        if feed.ArticleSummaryEnabled {
            article.SummaryStatus = "incomplete"
        }
    }

    return article
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 修正buildArticleFromEntry状态初始化逻辑</name>
  <files>backend-go/internal/domain/feeds/service.go</files>
  <behavior>
    - Test 1: Feed.ArticleSummaryEnabled=true, FirecrawlEnabled=false → article.SummaryStatus="pending"
    - Test 2: Feed.ArticleSummaryEnabled=true, FirecrawlEnabled=true → article.SummaryStatus="incomplete" (existing behavior preserved)
    - Test 3: Feed.ArticleSummaryEnabled=false, FirecrawlEnabled=false → article.SummaryStatus="complete" (existing behavior preserved)
    - Test 4: Feed.ArticleSummaryEnabled=false, FirecrawlEnabled=true → article.SummaryStatus="complete" (no summary needed)
  </behavior>
  <action>
    修改feeds/service.go中的buildArticleFromEntry函数（约173-194行）：

    **现有逻辑：**
    ```go
    article := models.Article{
        SummaryStatus: "complete",
    }
    if feed.FirecrawlEnabled {
        article.FirecrawlStatus = "pending"
        if feed.ArticleSummaryEnabled {
            article.SummaryStatus = "incomplete"
        }
    }
    ```

    **修改为：**
    ```go
    article := models.Article{
        SummaryStatus: "complete",
    }
    if feed.FirecrawlEnabled {
        article.FirecrawlStatus = "pending"
        if feed.ArticleSummaryEnabled {
            article.SummaryStatus = "incomplete"
        }
    } else if feed.ArticleSummaryEnabled {
        // STAT-03: 只开启摘要不开启Firecrawl时，设置为pending等待手动触发摘要
        article.SummaryStatus = "pending"
    }
    ```

    关键点：
    - 新增else分支处理ArticleSummaryEnabled=true但FirecrawlEnabled=false的情况（per D-03）
    - 保持现有FirecrawlEnabled=true时的逻辑不变
    - 使用"pending"而非"incomplete"，因为不需要等待Firecrawl完成

    不要修改其他逻辑，只添加else if分支。
  </action>
  <verify>
    <automated>go test ./internal/domain/feeds -run TestBuildArticleFromEntry -v</automated>
  </verify>
  <done>buildArticleFromEntry函数新增else if分支，TestBuildArticleFromEntry覆盖4种配置组合且全部通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 内部逻辑 | 无外部输入，纯内部状态计算 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | N/A | buildArticleFromEntry | accept | 纯内部状态计算，无安全风险 |
</threat_model>

<verification>
- go test ./internal/domain/feeds -run TestBuildArticleFromEntry -v
- go build ./...
</verification>

<success_criteria>
- TestBuildArticleFromEntry覆盖4种feed配置组合
- ArticleSummaryEnabled=true, FirecrawlEnabled=false时summary_status="pending"
- 其他配置组合行为不变
</success_criteria>

<output>
After completion, create `.planning/phases/03-状态一致性修复/03-01-SUMMARY.md`
</output>