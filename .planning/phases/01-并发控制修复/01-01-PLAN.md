---
phase: 01-并发控制修复
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend-go/internal/jobs/firecrawl.go
  - backend-go/internal/jobs/content_completion.go
  - backend-go/internal/jobs/preference_update.go
autonomous: true
requirements: [CONC-03]
user_setup: []
must_haves:
  truths:
    - "TriggerNow returns consistent error format when locked"
    - "HTTP status code uses http constant, not hardcoded"
  artifacts:
    - path: "backend-go/internal/jobs/firecrawl.go"
      provides: "TriggerNow error format"
      contains: "status_code: http.StatusConflict"
    - path: "backend-go/internal/jobs/content_completion.go"
      provides: "TriggerNow error format"
      contains: "status_code: http.StatusConflict"
    - path: "backend-go/internal/jobs/preference_update.go"
      provides: "TriggerNow error format"
      contains: "status_code: http.StatusConflict"
  key_links:
    - from: "handler.go"
      to: "TriggerNow()"
      via: "respondTriggerResult extracts status_code"
      pattern: "result\\[\"status_code\"\\]"
---

<objective>
Standardize TriggerNow error response format across all schedulers.

Purpose: Ensure consistent HTTP status codes when scheduler is already running.
Output: All schedulers use `http.StatusConflict` constant instead of hardcoded `409`.

**Implements D-09 (per CONTEXT.md):** status_code统一使用http常量（http.StatusConflict等），不硬编码
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-并发控制修复/01-CONTEXT.md
@.planning/phases/01-并发控制修复/01-RESEARCH.md

<interfaces>
<!-- Key types and patterns executor needs -->

From handler.go:178-183 (respondTriggerResult):
```go
statusCode := http.StatusOK
if rawCode, ok := result["status_code"].(int); ok {
    statusCode = rawCode
}
delete(result, "status_code")
```

From auto_refresh.go:305-314 (reference - already correct):
```go
if !s.executionMutex.TryLock() {
    return map[string]interface{}{
        "accepted":    false,
        "started":     false,
        "reason":      "already_running",
        "message":     "...",
        "status_code": http.StatusConflict,  // <-- CORRECT pattern
    }
}
```

Files to fix (current - incorrect):
- firecrawl.go:67: `"status_code": 409` → `http.StatusConflict`
- content_completion.go:106: `"status_code": 409` → `http.StatusConflict`
- preference_update.go:137: `"status_code": 409` → `http.StatusConflict`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix status_code in firecrawl.go</name>
  <files>backend-go/internal/jobs/firecrawl.go</files>
  <read_first>
    - backend-go/internal/jobs/firecrawl.go (see current TriggerNow implementation)
    - backend-go/internal/jobs/auto_refresh.go (reference: correct http.StatusConflict usage)
  </read_first>
  <behavior>
    - Test 1: Trigger firecrawl when already running → returns HTTP 409 (Conflict)
    - Test 2: Response contains accepted=false, reason="already_running"
  </behavior>
  <action>
    In firecrawl.go TriggerNow() method (line ~67):
    1. Change `"status_code": 409` to `"status_code": http.StatusConflict`
    2. Verify import "net/http" is present (should already exist)

    Per D-09: Use http constant, not hardcoded number.
  </action>
  <verify>
    <automated>go test ./internal/jobs -run TestTriggerNowStatusCode -v</automated>
  </verify>
  <done>
    firecrawl.go TriggerNow() contains "status_code": http.StatusConflict
    Hardcoded 409 removed from firecrawl.go
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix status_code in content_completion.go</name>
  <files>backend-go/internal/jobs/content_completion.go</files>
  <read_first>
    - backend-go/internal/jobs/content_completion.go (see TriggerNow at line 98-115)
    - backend-go/internal/jobs/auto_refresh.go (reference pattern)
  </read_first>
  <behavior>
    - Test 1: Trigger content_completion when already running → returns HTTP 409
    - Test 2: status_code is int type (http.StatusConflict = 409)
  </behavior>
  <action>
    In content_completion.go TriggerNow() method (line ~106):
    1. Change `"status_code": 409` to `"status_code": http.StatusConflict`
    2. Verify import "net/http" is present

    Per D-09: Use http constant, not hardcoded number.
  </action>
  <verify>
    <automated>go test ./internal/jobs -run TestTriggerNowStatusCode -v</automated>
  </verify>
  <done>
    content_completion.go TriggerNow() contains "status_code": http.StatusConflict
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fix status_code in preference_update.go</name>
  <files>backend-go/internal/jobs/preference_update.go</files>
  <read_first>
    - backend-go/internal/jobs/preference_update.go (see TriggerNow at line 127-149)
    - backend-go/internal/jobs/auto_refresh.go (reference pattern)
  </read_first>
  <behavior>
    - Test 1: Trigger preference_update when already running → returns HTTP 409
    - Test 2: status_code matches http.StatusConflict value
  </behavior>
  <action>
    In preference_update.go TriggerNow() method (line ~137):
    1. Change `"status_code": 409` to `"status_code": http.StatusConflict`
    2. Verify import "net/http" is present

    Per D-09: Use http constant, not hardcoded number.
  </action>
  <verify>
    <automated>go test ./internal/jobs -run TestTriggerNowStatusCode -v</automated>
  </verify>
  <done>
    preference_update.go TriggerNow() contains "status_code": http.StatusConflict
    All three schedulers now use http constant
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | Untrusted HTTP request params |
| API → Scheduler | Trusted internal call |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | TriggerNow response | mitigate | Use http.StatusConflict constant ensures correct HTTP semantics - clients receive proper error code |
| T-01-02 | Information Disclosure | Error message | accept | Chinese error messages are informational only, no sensitive data |
</threat_model>

<verification>
After all tasks:
1. Run: `go test ./internal/jobs -run TestTriggerNowStatusCode -v`
2. All three files grep: `grep -n "http.StatusConflict" firecrawl.go content_completion.go preference_update.go`
3. Confirm no hardcoded 409 remains: `grep -n "status_code.*409" firecrawl.go content_completion.go preference_update.go` should return empty
</verification>

<success_criteria>
- [ ] firecrawl.go uses http.StatusConflict
- [ ] content_completion.go uses http.StatusConflict
- [ ] preference_update.go uses http.StatusConflict
- [ ] No hardcoded 409 in any scheduler TriggerNow
- [ ] Test passes: TestTriggerNowStatusCode
</success_criteria>

<output>
After completion, create `.planning/phases/01-并发控制修复/01-01-SUMMARY.md`
</output>