---
phase: 02-watched-tags-homepage-feed
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - front/app/api/topicGraph.ts
  - front/app/api/watchedTags.ts
  - front/app/features/shell/components/AppSidebarView.vue
  - front/app/features/shell/components/FeedLayoutShell.vue
  - front/app/features/topic-graph/components/TagHierarchy.vue
  - front/app/types/article.ts
autonomous: true
requirements: [WATCH-01, FEED-01, FEED-02, FEED-03]

must_haves:
  truths:
    - "用户可在主题图谱页点击心形图标关注/取消关注标签"
    - "关注状态即时反馈：点击后 UI 立即更新，后台同步 API"
    - "首页侧边栏显示'关注标签'分组，含'全部关注'入口和每个关注标签筛选器"
    - "点击'全部关注'显示混合文章流，默认按相关度排序"
    - "点击单个关注标签只显示该标签文章，按时间排序"
    - "无关注标签时侧边栏显示引导横幅，首页保持默认时间线"
  artifacts:
    - path: "front/app/api/watchedTags.ts"
      provides: "关注标签前端 API 层"
      exports: ["useWatchedTagsApi"]
    - path: "front/app/features/shell/components/AppSidebarView.vue"
      provides: "侧边栏关注标签分组"
      contains: "watched"
    - path: "front/app/features/shell/components/FeedLayoutShell.vue"
      provides: "首页关注标签筛选 + 文章加载逻辑"
      contains: "watched"
    - path: "front/app/features/topic-graph/components/TagHierarchy.vue"
      provides: "标签卡片心形关注图标"
      contains: "watch"
  key_links:
    - from: "front/app/features/shell/components/FeedLayoutShell.vue"
      to: "/api/articles?watched_tag_ids=..."
      via: "useArticlePagination with watched_tag_ids filter"
      pattern: "watchedTag"
    - from: "front/app/features/shell/components/AppSidebarView.vue"
      to: "front/app/api/watchedTags.ts"
      via: "loadWatchedTags on mount"
      pattern: "useWatchedTagsApi"
    - from: "front/app/features/topic-graph/components/TagHierarchy.vue"
      to: "/api/topic-tags/:id/watch"
      via: "watch/unwatch API calls"
      pattern: "toggleWatch"
---

<objective>
前端关注标签交互 + 首页侧边栏关注推送

Purpose: 完成用户可操作的完整流程：在主题图谱页关注标签 → 首页侧边栏看到关注分组 → 点击查看关注标签文章推送
Output: 前端 API 层 + 侧边栏关注分组 + 文章筛选逻辑 + 心形关注图标
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-watched-tags-homepage-feed/02-CONTEXT.md
@.planning/phases/02-watched-tags-homepage-feed/02-01-SUMMARY.md

<interfaces>
<!-- Plan 01 创建的后端 API -->
POST /api/topic-tags/:tag_id/watch → {success, data: {id, is_watched, watched_at}}
POST /api/topic-tags/:tag_id/unwatch → {success, data: {id, is_watched}}
GET /api/topic-tags/watched → {success, data: [{id, slug, label, category, watched_at, is_abstract, child_slugs}]}
GET /api/articles?watched_tag_ids=1,2&sort_by=relevance → 标准分页文章响应

<!-- 前端侧边栏 Props 和 Emits -->
From front/app/features/shell/components/AppSidebarView.vue:
```typescript
interface Props {
  sidebarCollapsed?: boolean
  sidebarWidth?: number
  selectedCategory?: string | null
  selectedFeed?: string | null
  globalUnreadCount?: number
}

const emit = defineEmits<{
  categoryClick: [categoryId: string]
  feedClick: [feedId: string]
  favoritesClick: []
  aiSummariesClick: []
  // ... etc
}>()
```

<!-- 前端文章筛选类型 -->
From front/app/types/article.ts:
```typescript
export interface ArticleFilters {
  page?: number
  per_page?: number
  feed_id?: number
  category_id?: number
  uncategorized?: boolean
  read?: boolean
  favorite?: boolean
  search?: string
  start_date?: string
  end_date?: string
}
```

<!-- FeedLayoutShell 文章加载 -->
From front/app/features/shell/components/FeedLayoutShell.vue:
```typescript
const selectedCategory = ref<string | null>(null)
const selectedFeed = ref<string | null>(null)

function buildArticleFilters() {
  const filters: any = {}
  if (selectedFeed.value) {
    filters.feed_id = parseInt(selectedFeed.value)
  } else if (selectedCategory.value === 'favorites') {
    filters.favorite = true
  } else if (selectedCategory.value && selectedCategory.value !== 'ai-summaries') {
    filters.category_id = parseInt(selectedCategory.value)
  }
  // ...
  return filters
}
```

<!-- 现有 topicGraph API 模式 -->
From front/app/api/topicGraph.ts:
```typescript
export function useTopicGraphApi() {
  return {
    async searchTags(query: string, category?: string, limit?: number) {
      return apiClient.get<...>(withQuery('/topic-tags/search', {...}))
    },
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 前端关注标签 API + 主题图谱心形图标</name>
  <files>front/app/api/watchedTags.ts, front/app/api/topicGraph.ts, front/app/features/topic-graph/components/TagHierarchy.vue, front/app/types/article.ts</files>
  <action>
1. **创建 `front/app/api/watchedTags.ts`** — 关注标签 API 层（per D-09 每次从后端加载）:
   ```typescript
   import { apiClient } from './client'

   export interface WatchedTag {
     id: number
     slug: string
     label: string
     category: string
     watchedAt: string | null
     isAbstract: boolean
     childSlugs: string[]
   }

   export function useWatchedTagsApi() {
     return {
       async listWatchedTags() {
         const res = await apiClient.get<any>('/topic-tags/watched')
         if (!res.success) return res
         const data = (res.data || []).map((t: any) => ({
           id: t.id,
           slug: t.slug,
           label: t.label,
           category: t.category,
           watchedAt: t.watched_at,
           isAbstract: t.is_abstract || false,
           childSlugs: t.child_slugs || [],
         }))
         return { ...res, data }
       },
       async watchTag(tagId: number) {
         return apiClient.post(`/topic-tags/${tagId}/watch`, {})
       },
       async unwatchTag(tagId: number) {
         return apiClient.post(`/topic-tags/${tagId}/unwatch`, {})
       },
     }
   }
   ```

2. **扩展 `ArticleFilters`** — 在 `front/app/types/article.ts` 的 ArticleFilters 接口新增：
   ```typescript
   watched_tag_ids?: string  // 逗号分隔的 tag ID
   sort_by?: 'relevance' | 'date'
   ```

3. **TagHierarchy.vue 添加心形关注图标**（per D-01 在主题图谱页, D-02 心形图标, D-03 即时反馈）:
   - 在标签行（TagHierarchyRow）的操作区域（detach/reassign 按钮旁或标签名左侧）添加心形图标按钮
   - 使用 `@iconify/vue` 的 `mdi:heart` / `mdi:heart-outline`
   - 已关注状态：心形填满 + 红色 (`text-red-500`)，未关注：空心 + 默认色
   - 点击行为：
     - 立即翻转本地 watched 状态（即时反馈 per D-03）
     - 调用 `useWatchedTagsApi().watchTag(id)` 或 `unwatchTag(id)`
     - API 失败时回滚本地状态并显示 console.error
   - 标签数据需要新增 `isWatched` 字段标记。如果后端 watched 列表返回的数据可以和标签树数据做交叉匹配来判定关注状态
   - 注意：标签树数据来自 GetTagHierarchy API（返回结构已含 tag 信息），需在此组件挂载时额外加载一次 watched tags 列表，用于标记每个标签的关注状态

4. **topicGraph.ts 可选扩展** — 无需修改，watched tags API 在 watchedTags.ts 中独立定义
  </action>
  <verify>
    <automated>cd front && pnpm exec nuxi typecheck 2>&1 | tail -5</automated>
  </verify>
  <done>
    - useWatchedTagsApi composable 可用：listWatchedTags, watchTag, unwatchTag
    - ArticleFilters 支持 watched_tag_ids + sort_by
    - TagHierarchy 标签行有心形关注图标
    - 点击心形即时反馈，API 同步，失败回滚
    - typecheck 通过
  </done>
</task>

<task type="auto">
  <name>Task 2: 首页侧边栏关注标签分组 + 文章筛选</name>
  <files>front/app/features/shell/components/AppSidebarView.vue, front/app/features/shell/components/FeedLayoutShell.vue</files>
  <action>
1. **AppSidebarView.vue 添加关注标签分组**（per D-05~D-09, D-16~D-18）:

   新增 Props:
   - `watchedTags: WatchedTag[]` — 从父组件传入
   - `selectedWatchedTagId: string | null` — 当前选中的关注标签 ID（null=全部关注）

   新增 Emits:
   - `watchedTagsClick: []` — 点击"全部关注"
   - `watchedTagClick: [tagId: string]` — 点击单个关注标签

   侧边栏结构（在"主题图谱"按钮和分隔线之间插入关注标签分组）:
   ```
   [全部文章] [收藏夹] [AI总结] [日报周报] [主题图谱]
   ── 关注标签 ──────────
   [心形] 全部关注          ← 点击显示混合流
   [标签icon] 标签名1       ← 点击筛选单个标签
   [标签icon] 标签名2
   ── 分类 ──────────
   ...
   ```

   无关注标签时（per D-16, D-17）:
   - 显示引导横幅：文字"关注标签可获取个性化文章推送" + 链接按钮"前往关注"（点击导航到 /topics）
   - 引导横幅在侧边栏"关注标签"分组区域内显示，带浅色背景

   有关注标签时:
   - 显示"全部关注"入口（心形图标 `mdi:heart-multiple`）
   - 下方列出每个关注标签，使用标签 category 对应的图标（或 `mdi:tag`），标签名
   - 抽象标签用不同颜色或 `mdi:tag-multiple` 图标区分

   点击高亮状态:
   - selectedCategory === 'watched-tags' 且 selectedWatchedTagId === null → "全部关注"高亮
   - selectedCategory === 'watched-tags' 且 selectedWatchedTagId === tag.id → 对应标签高亮
   - 其他情况不高亮

2. **FeedLayoutShell.vue 添加关注标签筛选逻辑**（per D-07, D-08, D-11, D-12, D-18）:

   新增 state:
   ```typescript
   const watchedTags = ref<WatchedTag[]>([])
   const selectedWatchedTagId = ref<string | null>(null)
   ```

   新增加载函数:
   ```typescript
   async function loadWatchedTags() {
     const { useWatchedTagsApi } = await import('~/api/watchedTags')
     const api = useWatchedTagsApi()
     const res = await api.listWatchedTags()
     if (res.success && res.data) {
       watchedTags.value = res.data
     }
   }
   ```

   在 onMounted 中调用 `loadWatchedTags()`

   扩展 buildArticleFilters:
   ```typescript
   function buildArticleFilters() {
     const filters: any = {}
     if (selectedCategory.value === 'watched-tags') {
       if (selectedWatchedTagId.value) {
         // 单个标签筛选 per D-12: 按时间排序
         filters.watched_tag_ids = selectedWatchedTagId.value
         filters.sort_by = 'date'
       } else {
         // 全部关注 per D-11: 按相关度排序
         filters.watched_tag_ids = watchedTags.value
           .map(t => t.id)
           .join(',')
         filters.sort_by = 'relevance'
       }
     } else if (selectedFeed.value) {
       // ... 现有逻辑不变
     }
     // ...
     return filters
   }
   ```

   新增 handler 函数:
   ```typescript
   async function handleWatchedTagsClick() {
     selectedCategory.value = 'watched-tags'
     selectedFeed.value = null
     selectedWatchedTagId.value = null
     showAISummaries.value = false
     selectedSummary.value = null
     startDate.value = ''
     endDate.value = ''
     await loadArticles()
   }

   async function handleWatchedTagClick(tagId: string) {
     selectedCategory.value = 'watched-tags'
     selectedFeed.value = null
     selectedWatchedTagId.value = tagId
     showAISummaries.value = false
     selectedSummary.value = null
     startDate.value = ''
     endDate.value = ''
     await loadArticles()
   }
   ```

   无关注标签时（per D-18）:
   - selectedCategory === 'watched-tags' 且 watchedTags 为空 → loadArticles 不添加 watched_tag_ids filter，保持默认时间线

   在 handleCategoryClick/handleFeedClick/handleAllArticlesClick 等函数中，重置 selectedWatchedTagId = null

   传递 props 给 AppSidebarView:
   ```html
   <LayoutAppSidebar
     :watched-tags="watchedTags"
     :selected-watched-tag-id="selectedWatchedTagId"
     @watched-tags-click="handleWatchedTagsClick"
     @watched-tag-click="handleWatchedTagClick"
     ...
   />
   ```

   在每次进入侧边栏"关注标签"入口时重新加载 watched tags（per D-09）。实现方式：handleWatchedTagsClick 和 handleWatchedTagClick 中都调用 loadWatchedTags()。
  </action>
  <verify>
    <automated>cd front && pnpm exec nuxi typecheck 2>&1 | tail -5</automated>
  </verify>
  <done>
    - 侧边栏显示"关注标签"分组，含全部关注入口和每个关注标签
    - 点击全部关注显示按相关度排序的混合文章流
    - 点击单个标签显示该标签文章，按时间排序
    - 无关注标签时显示引导横幅，首页保持默认时间线
    - typecheck 通过
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | 关注标签 ID 从前端传递到后端 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | S | watchedTags API | accept | 单用户应用，无鉴权需求 |
| T-02-06 | E | TagHierarchy watch toggle | mitigate | 即时反馈 + API 失败回滚，避免状态不一致 |
| T-02-07 | I | watched_tag_ids param | mitigate | 使用 GORM 参数化查询（Plan 01 已处理），前端仅传 ID 数字字符串 |
</threat_model>

<verification>
1. `cd front && pnpm exec nuxi typecheck` — 类型检查通过
2. `cd front && pnpm build` — 构建通过
3. 手动验证流程：
   - 进入 /topics → 点击标签心形图标 → 图标变红填满
   - 进入首页 → 侧边栏显示"关注标签"分组 → 点击"全部关注"→ 看到文章列表
   - 点击某个关注标签 → 文章列表切换为该标签文章
   - 取消关注所有标签 → 侧边栏显示引导横幅 → 首页保持默认时间线
</verification>

<success_criteria>
1. 主题图谱页标签卡片有心形关注图标，点击即时反馈（per D-01~D-03）
2. 首页侧边栏有"关注标签"分组，全部关注+每个标签筛选器（per D-05~D-07）
3. 全部关注混合流默认按相关度排序，单标签筛选按时间排序（per D-11~D-12）
4. 无关注标签时引导横幅引导到主题图谱页（per D-16~D-17）
5. 无关注标签时首页默认时间线不变（per D-18）
6. typecheck + build 通过
</success_criteria>

<output>
After completion, create `.planning/phases/02-watched-tags-homepage-feed/02-02-SUMMARY.md`
</output>
